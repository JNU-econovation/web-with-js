class TimeConverter {
  constructor(time, timeUnit, convertUnit) {
    this.time = time; 
    this.timeUnit = timeUnit;
    this.convertUnit = convertUnit;
  }

  convertSeconds(time){
    
  }
};

module.exports = TimeConverter;


// s : 초, m : 분, h : 시, d : 일