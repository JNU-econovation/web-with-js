class Parser {
  constructor() {
    this.baseUnit = "";
    this.convertTargetTime = "";
  }

  parseCommand(command) {
    try {
    } catch (error) {}
  }

  parseBaseUnit() {}

  parseConvertTargetTime() {}
}

module.exports = Parser;
